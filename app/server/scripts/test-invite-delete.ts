// Retired unsafe test entry: never imports a database or copies a route handler.
console.error("Use pnpm test from the repository root. Tests inject real routes and isolated databases.");
process.exitCode = 1;
